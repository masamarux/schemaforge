[project]
name = "schemaforge"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pydantic>=2.0.0", "typer>=0.12.0"]

[project.scripts]
schemaforge = "schemaforge.cli:app"

[tool.pytest.ini_options]
pythonpath = ["src"]
